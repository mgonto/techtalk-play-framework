# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                                       Application.index

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/                                staticDir:public

GET /agenda/new                                 Agenda.newContact
POST /agenda/create                             Agenda.create
GET /agenda/list                                Agenda.list
GET /annoy/start/{id}                            Annoy.start
GET /annoy/stop/{id}                            Annoy.stop
GET /agenda/edit/{id}                           Agenda.edit
POST /agenda/edit                               Agenda.save

*       /{controller}/{action}                  {controller}.{action}